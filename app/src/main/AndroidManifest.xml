<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 📌 Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 📌 Sensor permissions -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- 📌 Internet permission for sharing -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 📌 Needed if reminders should survive reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyHealthWellness">

        <!-- ✅ Splash + Theme handling -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Other activities -->
        <activity android:name=".ui.activities.AddMoodActivity" android:exported="false" />
        <activity android:name=".ui.activities.AddTaskActivity" android:exported="false" />
        <activity android:name=".ui.activities.Home" android:exported="false" />
        <activity android:name=".ui.activities.Login" android:exported="false" />
        <activity android:name=".ui.activities.Register" android:exported="false" />
        <activity android:name=".ui.activities.perMeditate" android:exported="false" />
        <activity android:name=".ui.activities.perJournaling" android:exported="false" />
        <activity android:name=".ui.activities.perHydrating" android:exported="false" />
        <activity android:name=".ui.activities.perDailyHabit" android:exported="false" />

        <!-- ✅ Home-screen widget -->
        <receiver android:name=".ui.widgets.HabitWidget"
            android:label="Habit Progress"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habit_widget_info" />
        </receiver>

        <!-- ✅ Boot receiver (restart reminders after reboot) -->
        <receiver android:name=".ui.receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
