<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.DailyHealthWellness" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <!-- Example: <item name="colorPrimary">@color/teal_700</item> -->
    </style>

    <style name="Theme.DailyHealthWellness" parent="Base.Theme.DailyHealthWellness" />

    <!-- Habit Card Custom Style -->
    <style name="HabitCardStyle" parent="CardView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">90dp</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardBackgroundColor">#A7E4D8</item>
    </style>

</resources>
